{
  "name": "memo-app",
  "version": "1.0.0",
  "description": "React + Vercel Functions + PostgreSQL memo app",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
